{
  "name": "careerai-platform",
  "version": "1.0.0",
  "description": "CareerAI: AI Career Intelligence Platform - Full-stack React + Node.js Express",
  "scripts": {
    "start": "node backend/server.js",
    "build": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build",
    "server": "node backend/server.js",
    "client": "npm run dev --prefix frontend",
    "test": "node backend/test_endpoints.js"
  }
}
